+# 코드 발표 설명서

이 문서는 황승재 Computer Vision Portfolio의 발표 순서, 시연 방법, 핵심 코드 설명과 예상 질문 답변을 정리한 자료입니다.

## 1. 발표 핵심 한 문장

> 순수 HTML, CSS, JavaScript로 반응형 포트폴리오를 만들고, 사용자 이벤트가 상태를 변경한 뒤 DOM이 다시 렌더링되는 흐름을 구현했습니다.

## 2. 권장 발표 순서(약 5~7분)

### 1) 프로젝트 소개 — 30초

안녕하세요. 컴퓨터 비전 직무를 희망하는 황승재입니다.

이 프로젝트는 외부 UI 프레임워크 없이 HTML, CSS, JavaScript만 사용한 반응형 포트폴리오입니다. 컴퓨터 비전 관련 기술 스택과 자기소개를 보여주고, GitHub API를 호출해 실제 공개 저장소를 프로젝트 카드로 표시합니다.

### 2) 프로젝트 구조 — 30초

역할이 섞이지 않도록 파일을 다음과 같이 분리했습니다.

- `index.html`: 페이지의 의미와 콘텐츠 구조
- `css/style.css`: 레이아웃, 반응형 화면, 다크 모드와 시각 효과
- `js/script.js`: 상태 관리, 이벤트, DOM 변경, API와 폼 전송
- `images/`: 프로필 이미지와 제출용 화면 캡처

이렇게 분리하면 디자인이나 동작을 수정할 때 다른 역할의 코드를 덜 건드릴 수 있습니다.

### 3) HTML과 접근성 — 40초

페이지는 `header`, `nav`, `main`, `section`, `article`, `footer` 같은 시맨틱 태그로 구성했습니다.

시맨틱 태그를 사용한 이유는 브라우저, 검색 엔진, 스크린 리더가 각 영역의 의미를 이해할 수 있기 때문입니다. 프로필 이미지에는 의미 있는 `alt`를 넣고, 폼의 `label for`와 입력 요소의 `id`를 연결했습니다. 메뉴 및 상태 버튼에는 `aria-label`, `aria-expanded`, `aria-live`도 적용했습니다.

### 4) CSS와 반응형 설계 — 50초

모바일 화면을 기본으로 만들고 다음 브레이크포인트에서 레이아웃을 확장했습니다.

- 태블릿: 768px
- 데스크톱: 1024px

내비게이션처럼 한 방향으로 정렬하는 요소에는 Flexbox를 사용했습니다. 프로젝트 카드처럼 행과 열을 함께 다루는 영역에는 Grid의 `auto-fit`과 `minmax()`를 사용했습니다.

색상, 간격, 그림자, 전환 시간은 `:root`의 CSS 변수로 관리합니다. 다크 모드에서는 `data-theme="dark"`일 때 변수 값만 변경하므로 컴포넌트별 색상을 다시 작성할 필요가 없습니다.

### 5) JavaScript 상태와 렌더링 — 1분 30초

JavaScript의 핵심은 **이벤트 → 상태 변경 → 화면 업데이트**입니다.

예를 들어 다크 모드는 다음 순서로 동작합니다.

1. 사용자가 테마 버튼을 클릭합니다.
2. `state.theme` 값을 변경합니다.
3. `renderTheme()`이 HTML의 `data-theme`와 버튼 설명을 변경합니다.
4. 선택한 테마를 `localStorage`에 저장합니다.
5. 새로고침하면 저장된 테마를 다시 읽어 같은 화면을 유지합니다.

모바일 메뉴도 같은 구조입니다.

1. 햄버거 버튼의 `click` 이벤트를 감지합니다.
2. `state.menuOpen`을 변경합니다.
3. `renderMenu()`이 `active` 클래스와 ARIA 속성을 갱신합니다.

폼은 입력 이벤트가 발생할 때 값을 상태에 저장하고, 검증 결과에 따라 각 입력칸 주변의 오류 메시지를 렌더링합니다.

### 6) GitHub API — 1분

`fetch`와 `async/await`로 GitHub 공개 저장소 API를 호출합니다.

API를 호출하기 전에 상태를 `loading`으로 바꿔 스피너를 보여줍니다. 응답이 성공하면 필요한 데이터만 `map()`으로 변환해 카드 HTML을 만듭니다. 실패하면 `catch`에서 `error` 상태로 변경하고 재시도 버튼을 보여줍니다. 저장소가 없다면 별도의 빈 상태를 출력합니다.

상태는 다음 네 가지입니다.

- loading: 데이터를 요청하는 중
- success: 프로젝트 카드 표시
- error: 오류 안내와 다시 시도 버튼
- empty: 표시할 공개 저장소가 없음

언어 필터는 저장소의 대표 언어를 모아 버튼을 만들고, `filter()`로 선택한 언어의 프로젝트만 표시하는 보너스 기능입니다.

GitHub API에서 받은 문자열은 `innerHTML`에 넣기 전에 `escapeHtml()`로 이스케이프해 HTML 코드로 해석되지 않도록 했습니다.

### 7) 문의 폼 — 50초

폼은 이름, 이메일, 메시지를 검증합니다.

- 이름: 두 글자 이상
- 이메일: 이메일 형식 확인
- 메시지: 10자 이상

검증을 통과하면 FormSubmit으로 내용을 전달합니다. 전송 중에는 버튼을 비활성화해 중복 제출을 막고, 10초가 지나면 타임아웃 오류를 표시합니다. 최초 사용 시에는 일반 오류가 아니라 FormSubmit 활성화 안내를 보여줍니다.

브라우저 코드에 Gmail 비밀번호나 비밀 API 키를 넣지 않았습니다. 입력 내용이 외부 서비스를 거친다는 사실도 폼 아래에 표시했습니다.

### 8) 마무리 — 30초

이 프로젝트를 통해 시맨틱 HTML, 반응형 CSS, DOM 이벤트, 상태 관리, 비동기 API 처리와 예외 상황 UI를 직접 구현했습니다. 특히 React를 사용하지 않고도 이벤트와 상태가 화면 변경으로 연결되는 기본 원리를 이해하는 데 집중했습니다.

## 3. 권장 시연 순서

발표 중에는 다음 순서로 짧게 시연합니다.

1. 데스크톱 첫 화면과 각 섹션을 보여줍니다.
2. 내비게이션을 눌러 부드러운 스크롤을 보여줍니다.
3. 다크 모드를 켠 후 새로고침해 설정 유지 여부를 보여줍니다.
4. 브라우저 폭을 줄여 햄버거 메뉴를 보여줍니다.
5. Projects에서 GitHub 저장소와 언어 필터를 보여줍니다.
6. 빈 폼을 제출해 필드별 오류를 보여줍니다.
7. 올바른 값을 입력해 실제 문의 전송 성공 상태를 보여줍니다.
8. 아래로 스크롤해 TOP 버튼이 나타나고 맨 위로 이동하는지 보여줍니다.

실제 이메일 전송 시연은 FormSubmit이 활성화된 상태에서 수행합니다. 발표 직전 테스트 메시지를 한 번 보내 수신 여부를 확인합니다.

## 4. 주요 코드 설명 포인트

### `defer`를 사용하는 이유

HTML 파싱을 막지 않고 JavaScript 파일을 내려받으며, DOM 구성이 끝난 후 스크립트를 실행하기 위해 사용했습니다. 따라서 스크립트를 `body` 마지막에 넣지 않아도 DOM 요소를 안전하게 선택할 수 있습니다.

### `querySelector`와 `addEventListener`

`querySelector`는 제어할 DOM 요소를 찾습니다. `addEventListener`는 그 요소에서 발생하는 클릭, 입력, 제출 등의 이벤트에 함수를 연결합니다.

### `map()`, `filter()`, `forEach()`

- `map()`: GitHub API 데이터를 화면용 객체와 카드 HTML로 변환
- `filter()`: 선택한 언어에 맞는 프로젝트만 추출
- `forEach()`: 메뉴 링크, 폼 필드, 관찰 대상 요소에 동일 작업 수행

### `async/await`와 `try/catch/finally`

네트워크 요청 결과를 기다리는 코드를 순서대로 읽을 수 있게 작성했습니다. 정상 응답은 `try`, 오류는 `catch`, 성공 여부와 상관없이 필요한 정리는 `finally`에서 처리합니다.

### Intersection Observer

모든 스크롤 위치를 계속 계산하는 대신 요소가 화면에 들어오는 시점을 브라우저가 알려주도록 했습니다. 임계값은 `0.2`이며, 요소의 약 20%가 보이면 애니메이션을 적용합니다.

### `requestAnimationFrame`

스크롤 이벤트는 매우 자주 발생합니다. 매번 DOM을 변경하지 않고 다음 화면 갱신 시점에 한 번만 처리해 불필요한 렌더링을 줄였습니다.

## 5. 예상 질문과 답변

### Q1. Flexbox와 Grid의 차이는 무엇인가요?

Flexbox는 한 방향의 정렬에 적합하고 Grid는 행과 열을 함께 다루는 2차원 배치에 적합합니다. 그래서 내비게이션에는 Flexbox, 프로젝트 카드에는 Grid를 사용했습니다.

### Q2. 왜 상태 객체를 만들었나요?

화면에 영향을 주는 값을 한곳에서 관리하면 이벤트 처리 코드와 화면 변경 코드를 분리할 수 있습니다. 이는 React의 상태와 렌더링 구조를 이해하는 기초가 됩니다.

### Q3. `localStorage`는 왜 사용했나요?

사용자가 선택한 다크 모드를 브라우저에 저장해 새로고침하거나 다시 방문해도 설정이 유지되도록 하기 위해 사용했습니다.

### Q4. GitHub API가 실패하면 어떻게 되나요?

`try/catch`에서 오류를 처리해 스택트레이스를 노출하지 않고 안내 문구와 다시 시도 버튼을 보여줍니다. 403이면 API 요청 한도 초과 안내를, 10초가 지나면 타임아웃 안내를 보여줍니다.

### Q5. 빈 상태는 어떻게 확인할 수 있나요?

API 요청은 성공했지만 공개 저장소 배열이 비어 있으면 `success` 상태와 빈 배열을 확인해 “표시할 프로젝트가 없습니다”라는 UI를 렌더링합니다.

### Q6. `innerHTML` 사용은 안전한가요?

외부 API 문자열을 그대로 넣으면 안전하지 않을 수 있습니다. 이 프로젝트에서는 `escapeHtml()`로 특수문자를 치환하고 GitHub 링크가 허용된 주소로 시작하는지도 확인합니다.

### Q7. FormSubmit을 사용한 이유는 무엇인가요?

GitHub Pages는 정적 호스팅이라 서버에서 Gmail을 직접 발송할 수 없습니다. 비밀번호를 프론트엔드에 노출하지 않으면서 실제 전송을 구현하기 위해 정적 폼 백엔드를 사용했습니다.

### Q8. 외부 서비스 없이 이메일을 보낼 수 있나요?

`mailto:`는 가능하지만 방문자의 기본 메일 프로그램에 의존하며 자동 전송이 아닙니다. 안정적인 자동 전송에는 외부 폼 서비스나 직접 만든 백엔드가 필요합니다.

### Q9. 접근성을 위해 무엇을 했나요?

시맨틱 태그, 이미지 `alt`, 폼 `label`, 키보드 포커스 스타일, ARIA 상태, 본문 바로가기 링크, 동작 감소 설정을 적용했습니다.

### Q10. 개선한다면 무엇을 추가하겠나요?

프로젝트별 대표 이미지와 상세 페이지를 추가하고, 서버리스 함수로 문의를 처리해 외부 폼 서비스 의존도를 줄일 수 있습니다. 자동화 테스트와 Lighthouse 점검도 추가할 수 있습니다.

## 6. 제출 직전 체크리스트

- [x] HTML, CSS, JavaScript, 이미지 역할 분리
- [x] Hero, About, Skills, Projects, Contact, Footer
- [x] 모바일·태블릿·데스크톱 반응형
- [x] 햄버거 메뉴
- [x] 다크 모드 및 localStorage
- [x] 부드러운 스크롤과 TOP 버튼
- [x] Intersection Observer 애니메이션
- [x] 폼 유효성 검사와 실제 전송
- [x] GitHub API 로딩·성공·에러·빈 상태
- [x] 언어별 프로젝트 필터
- [x] README 설명과 스크린샷
- [x] GitHub 저장소 커밋
- [ ] GitHub Pages 활성화 후 배포 URL에서 최종 확인

## 7. 발표 전 주의사항

- GitHub API는 비인증 요청 한도가 있으므로 발표 직전에 반복 새로고침하지 않습니다.
- FormSubmit 수신 이메일을 미리 활성화하고 테스트 메일을 한 번 확인합니다.
- GitHub Pages를 활성화한 다음 배포 URL을 README에서 다시 확인합니다.
- 데스크톱, 모바일, 다크 모드 화면을 각각 한 번씩 확인합니다.

